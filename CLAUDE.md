# CLAUDE.md — 项目上下文与开发约定

> 本文件由前期产品调研对话蒸馏而来。修改架构决策前先阅读本文件和 docs/ 下的两张图。

## 1. 产品概念

一个「日记 + 代理社交」App:
- 用户写私密日记 → 训练个人数字化身(agent)
- 化身在社交区与其他用户的化身对话,产出契合度报告
- 双向确认后解锁与真人的对话
- 目标用户:社交退缩人群;核心价值主张:AI 社交是通向真人关系的漏斗,不是替代品

**市场定位结论**(2026-07 调研):
- 已验证的邻近产品:MindBank/Sensay(数字分身工具,无社交)、Butterflies(AI 角色社交,非真人化身)、Moltbook(agent 互聊,无真人回流)
- 本产品差异化 = 日记作为高质量训练素材 + 「解锁真人」终点,市场空白
- 已知风险:化身保真度(死穴)、孤独感悖论、冷启动、日记数据合规(PIPL/生成式AI管理办法)

## 2. 核心技术方案:风格蒸馏

风格分两层,手段不同:

**表层风格**(措辞/句长/语气/口头禅)→ 微调:
- 日记切片后用强模型逆向构造「情境→用户口吻回应」对话对(关键步骤,否则学成日记腔)
- LoRA 微调开源基座(7B–14B)
- 文体计量学指标验证(句长/词汇丰富度/标点/功能词分布)

**深层人格**(观点/价值排序/幽默方式)→ 结构化蒸馏 + RAG:
- LLM 从日记流抽取人格断言卡片:带时间戳 + 置信度 + 出处
- 对话时检索卡片注入,不检索原始日记(信噪比)
- 新断言覆盖旧断言(人会变)

**两个配套机制**:
- 主动提问引擎:识别卡片库低置信区域,每日反问用户 1 题(同时解决日记留存)
- DPO 对比校准:用户标注「像我/不像我」偏好对,磨掉基座模型的 AI 腔(anti-sycophancy)

**北极星指标**:图灵式盲测——用户看化身回应,判断「这是不是我会说的话」的通过率。

## 3. MVP 阶段与当前范围

| 阶段 | 内容 | 硬性门槛 |
|---|---|---|
| Phase 0 (当前) | 日记录入 App + 蒸馏 pipeline v1 + 单人化身 + 自对话界面 | 盲测通过率 ≥ 60% |
| Phase 1 | 主动提问 + 反馈UI + DPO 训练环 | 通过率 ≥ 80%,7日留存 ≥ 40% |
| Phase 2 | 匹配 + Agent↔Agent 沙盒(50-100人内测) | 「想认识这个人」≥ 30% |
| Phase 3 | 真人解锁 + 合规 | 解锁后真人对话持续 ≥ 3 天比例 |

**重要约束:当前只做 Phase 0。不要提前实现匹配、agent 互聊、解锁等 Phase 2/3 功能,哪怕顺手。**

## 4. 技术栈与架构约定

- **客户端**:Expo (React Native),TypeScript。薄客户端原则——所有模型逻辑、蒸馏、检索走后端 API,客户端只做 UI 和状态。
- **开发环境**:Windows 原生(不用 WSL,避免 Expo dev server 局域网问题);iPad 跑 Expo Go 真机预览;构建走 EAS Build 云端,分发走 TestFlight。
- **后端**:FastAPI (Python)。蒸馏 pipeline、LoRA/DPO 训练、RAG 检索均为后端服务,GPU 训练任务异步化。
- **禁止事项**:客户端不得直接调用 LLM API(密钥安全 + 逻辑收敛后端);不要引入 Redux(用 zustand);聊天 UI 优先 react-native-gifted-chat。

## 5. 数据与隐私红线

- 日记是最高敏感级数据:传输 TLS、存储加密、蒸馏 pipeline 入口先做隐私实体脱敏
- 人格卡片必须保留出处链接(可追溯、可删除)
- 用户删除日记 → 级联删除衍生的对话对与人格卡片
- 商业化前需解决:国区上架的 ICP 备案(需国内主体)、生成式 AI 备案;MVP 阶段先走美区/澳区 TestFlight

## 6. 相关文档

- `docs/architecture.mermaid` — 五层系统架构图(数据/蒸馏/模型/应用/评估环)
- `docs/mvp-roadmap.mermaid` — 四阶段规划图与门槛指标
